# Job Hunter Agent — Environment Variables
# Copy this file to .env and fill in your values

# --- Required ---
JH_ANTHROPIC_API_KEY=sk-ant-...        # Anthropic API key
JH_TAVILY_API_KEY=tvly-...             # Tavily search API key

# --- Database (optional, defaults to SQLite) ---
# JH_DB_BACKEND=sqlite                 # "sqlite" (default) or "postgres"
# JH_DATABASE_URL=sqlite+aiosqlite:///./job_hunter.db
# JH_POSTGRES_URL=postgresql+asyncpg://postgres:dev@localhost:5432/jobhunter

# --- Embeddings (optional, defaults to local) ---
# JH_EMBEDDING_PROVIDER=local          # "local" (default) or "voyage"
# JH_VOYAGE_API_KEY=                    # Required if embedding_provider=voyage
# JH_EMBEDDING_MODEL=all-MiniLM-L6-v2  # Local model name
# JH_EMBEDDING_DIMENSION=384           # 384 for MiniLM, 1024 for Voyage

# --- Email (optional) ---
# JH_EMAIL_PROVIDER=smtp               # "smtp" (default) or "sendgrid"
# JH_SENDGRID_API_KEY=                  # Required if email_provider=sendgrid
# JH_SMTP_HOST=smtp.gmail.com
# JH_SMTP_PORT=587
# JH_SMTP_USER=
# JH_SMTP_PASSWORD=

# --- LangSmith (optional) ---
# JH_LANGSMITH_API_KEY=                 # Optional: LangSmith tracing
# JH_LANGSMITH_PROJECT=job-hunter-agent

# --- OpenTelemetry (optional) ---
# JH_OTEL_EXPORTER=none              # "none" (off), "console", or "otlp"
# JH_OTEL_ENDPOINT=http://localhost:4317  # OTLP collector endpoint
# JH_OTEL_SERVICE_NAME=job-hunter-agent

# --- Scraping ---
# JH_MAX_CONCURRENT_SCRAPERS=5
# JH_SCRAPE_TIMEOUT_SECONDS=30

# --- Cost Guardrails ---
# JH_MAX_COST_PER_RUN_USD=5.0          # Hard stop if exceeded
# JH_WARN_COST_THRESHOLD_USD=2.0       # Warning threshold

# --- Docker Compose Overrides ---
# The following are auto-set by docker-compose.yml for the `app` service.
# Do NOT set these in .env when running via Docker Compose:
# JH_DATABASE_URL, JH_REDIS_URL, JH_LOG_FORMAT, JH_OUTPUT_DIR, JH_CHECKPOINT_DIR
